import React from "react";
// import { Button } from "antd";
// import UserTable from "../../components/interface/UserTable/UserTable.tsx";
// import UserForm from "../../components/interface/UserForm/UserForm.tsx";
// import { useUsers } from "../../api/hooks/users/getUsers.ts";
// import styles from "./users.module.css";

 const Users: React.FC = () => {
  // const { data, loading, visible, setVisible, addUser } = useUsers();

  // const handleAdd = () => {
  //   setVisible(true);
  // };

  // const handleCancel = () => {
  //   setVisible(false);
  // };

  return (
    <>
      {/* <h2 className={styles.title}>Таблица с данными о пользователях</h2>
      <Button
        className={styles.primary__btn}
        type="primary"
        onClick={handleAdd}
        style={{ marginBottom: 16 }}
      >
        Добавить
      </Button>
      <UserForm visible={visible} onSubmit={addUser} onCancel={handleCancel} />
      <UserTable data={data} loading={loading} /> */}
    </>
  );
};

export default Users;

